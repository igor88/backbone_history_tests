var assert = require("assert")
describe('Array2', function(){
  describe('#indexOf()2', function(){
    it('should return -1 when the value is not present  qw', function(){
      assert.equal(-1, [1,2,3].indexOf(5));
      assert.equal(-1, [1,2,3].indexOf(0));
    })
  })
})

